import React from 'react';
import { Button } from 'react-native-elements';

export const SuccessButton = () => (
    <Button title="SUCCESS" type="solid" buttonStyle={{ backgroundColor: 'green' }} />
);
